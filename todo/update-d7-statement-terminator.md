# Task: Update D7 Statement terminator

The decision D7 (L136-147) states the statement terminator is `;`. Verify if `spec/syntax.md` uses `;` for statement termination.

Task: Use `;` as statement terminator.
Affected Files: `spec/syntax.md`.
