# Task: D34 `else if` ladder

Document that `else if` is always treated as a ladder rung. A nested `if` inside an `else` branch requires an explicit `start` block for disambiguation.

Affected Files: `spec/control.md`, `spec/syntax.md`
