# Task: Update D87 Built-in keyword kind

The decision D87 (L1571-1608) specifies the built-in is `kind`, replacing `type`. Update all occurrences of `type` as a keyword in `spec/types.md` and `spec/syntax.md`.

Task: Rename keyword `type` to `kind`.
Affected Files: `spec/types.md`, `spec/syntax.md`.
