# Task: D9 `∈` as membership vs type test

Clarify that `∈` is resolved at compile time: it is a type test if the right operand is a type, otherwise it is a membership test.

Affected Files: `spec/types.md`, `spec/syntax.md`.
