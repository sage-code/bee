# Task: D22 Implicit coercion in string concatenation

Document that in `a + b`, if either operand is `S`, the other is implicitly converted to its default display representation.

Affected Files: `spec/types.md`, `spec/processing.md`
