# Task: Update D24 Identity operator !=

The decision D24 (L363-379) defines `!=` as identity, not value inequality. Ensure the documentation in `spec/syntax.md` or `spec/rules.md` reflects this specific behavior.

Task: Update documentation for `!=` as identity operator.
Affected Files: `spec/syntax.md`, `spec/rules.md`.
