# Task: D87 Rename `type` keyword to `kind`

Document the replacement of the built-in keyword `type` with `kind`. Ensure all documentation references to introspection or type labeling use `kind`.

Affected Files: `spec/types.md`, `spec/syntax.md`
