# Task: D31 Parameter lists

Document that parameter lists are flat, comma-separated lists of `[*] name [: default] [∈ type]`. Types are propagated leftward to untyped preceding items.

Affected Files: `spec/syntax.md`, `spec/functions.md`
