# Task: D30 Bracket meaning

Document that `[]` bracket meaning is positional: array type (if after `∈`/`<:` or type desc), index/slice (if after primary expr), or array literal (elsewhere).

Affected Files: `spec/syntax.md`, `spec/types.md`
