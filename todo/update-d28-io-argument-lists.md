# Task: D28 Parenthesised argument list after I/O

Document that immediately after `print`, `write`, or `read`, a parenthesised comma list is interpreted as an argument list, not a list literal.

Affected Files: `spec/syntax.md`, `spec/processing.md`
