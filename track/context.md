# Project Context: Bee Language

Bee is an experimental, rule-based, compiled, static-typed, and modular programming language designed by Sage-Code Laboratory.

## Current Priorities
1. Finalizing language specification (currently moving documentation from HTML to Markdown in `/spec`).
2. Aligning documentation with settled decisions from `DECISIONS.md`.
3. Tracking all tasks via the `/todo` folder.

## Key Constraints
- Documentation is being converted from HTML to Markdown.
- All new/modified code or specs must strictly follow the `DECISIONS.md` log.
- All changes must be tracked in the `/todo` folder.
- Follow the execution constraints defined in `GEMINI.md` (no parallel operations, mandatory sleep between writes).
- License: GPLv3.
